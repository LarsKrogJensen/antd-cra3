mutation CreateEnvironment($input: CreateEnvironmentInput!) {
    createEnvironment(input: $input) {
        environment {
            id
            shortName
            longName
            team
        }
    }
}
